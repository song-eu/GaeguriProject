# import Error from '../../shared.graphql'

type Mutation{
    participateProject(input: PP_User): [Error]
}

input PP_User {
    User_id: Int, 
    Project_id: Int, 
    Position_id: Int, 
    Position_name: String,
    Answer: String
} 